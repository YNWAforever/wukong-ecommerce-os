# Package J — Accessibility, Responsive, Security and Performance Hardening — Design

**Date:** 2026-09-02
**Status:** Approved (brainstorming), pending implementation plan
**Parent plan:** `docs/superpowers/plans/2026-08-30-wukong-catalog-operations-os-integration.md` — Package J (§16). Dependency graph: depends on "all of B–I." Packages B, C, D, E, F, G are merged into `main`; Packages H and I were fully built in a prior session but never actually merged (they were stacked into each other's branches, never targeting `main` directly) — this gap was discovered and fixed via PR #63 (`claude/integrate-packages-h-i`, CI-green, pending the user's own merge). This branch is built directly on top of that integration branch's tip, matching the established precedent from Package D building on Package B's unmerged tip when it needed B's real committed code.

## 1. What this builds

A genuine audit-and-fix pass, not a redo of what earlier packages already delivered. Every item below was independently, directly verified against the real current codebase (not assumed from the master plan's original §14/§15 checklist, which was written before any of B–I existed) via a parallel research audit across six dimensions: shell/landmark structure, form/live-region accessibility, table/card accessibility, contrast/touch-targets, security bounds/RLS test coverage, and observability/logging. Confirmed-already-done items (contrast ratios, keyboard focus-trap/restoration, icon naming — the codebase has no icons at all, log redaction, cross-workspace RLS tests, form-metadata association) are listed in §7 as explicitly out of scope, not silently assumed.

Ten real, confirmed gaps remain, falling into three groups: accessibility fixes (§2), a security bound (§3), and new observability infrastructure the master plan explicitly assigns to this package (§4–§5) rather than splitting into its own effort, since both are genuinely small additions reusing an existing mechanism (the audit event log's schema-less JSONB metadata), not a new platform.

## 2. Accessibility fixes

- **No skip link on any auth route.** `apps/web/components/auth-shell.tsx` has no equivalent of the `(app)` shell's `<a className="skip-link" href="#main-content">` (confirmed present at `apps/web/app/(app)/layout.tsx:23-25`). Add the same pattern to `AuthShell`, pointing at a new `id="main-content"` on its `<main className="auth-shell-card-wrap">`.
- **Duplicate `<h1>` on every auth page.** `AuthShell`'s marketing tagline (`auth-shell.tsx:79`, e.g. "Verify the evidence before approving the content.") renders as an `<h1>` inside the `<aside>`, and `AuthForm`'s real per-mode heading (`auth-form.tsx:308`, `<h1 id="auth-title">`) renders inside `<main>` — two H1s per page. The tagline is marketing copy, not a page heading; change it to a `<p>` (matching the surrounding `<p className="auth-shell-eyebrow">`/`<p>` siblings' semantic level). `AuthForm`'s heading remains the page's one real H1.
- **No route uses Next.js's `loading.tsx` convention anywhere** (confirmed via `Glob apps/web/**/loading.tsx` → zero matches across all ~13 routes). Two ad-hoc client-side "Loading…" states exist (`dashboard-listings-client.tsx`, `catalog-control-center.tsx`) but most routes have no loading affordance at all. Add a `loading.tsx` to every route: `dashboard`, `catalog`, `queue`, `batches`, `batches/[id]`, `listings/new`, `listings/import`, `listings/[id]`, `admin`, `jobs`, `quality`, `system-map`, and one shared `loading.tsx` for the `(auth)` route group. All reuse one small, shared skeleton/spinner component rather than 13 bespoke ones.
- **`AuthForm`'s password hint and inline errors aren't `aria-describedby`-associated with their inputs** (confirmed: the password input at `auth-form.tsx:342-355` has no `aria-describedby`; its `<small>Use 12 to 128 characters.</small>` hint has no `id` at all — pure visual proximity, no programmatic link). Add an `id` to the hint and reference it via `aria-describedby` on the password and confirm-password inputs.
- **`listing-queue.tsx`'s disabled checkbox label doesn't distinguish rows.** When a checkbox is disabled (the listing isn't yet approvable), its label is `` `${item.openBlockingFlagCount} 個未解決的合規標記` `` (`listing-queue.tsx:83`) — two rows with the same flag count get identical labels, so a screen-reader user can't tell them apart. Prepend `item.title`, matching the enabled-state label's own pattern (`` `選取 ${item.title}` ``, line 80-83).
- **`catalog-control-center.tsx`'s `<table>` has no accessible name of its own** (only inherits one from the ancestor `<section aria-label="商品控制中心">`, one level removed). Add `aria-label` directly on the `<table>` element.
- **Two real touch-target failures**, found via direct WCAG contrast/sizing computation, not assumed-passing: `.queue-action` is `min-height: 42px` (`globals.css:654`), below the 44px minimum, and overrides `.secondary-button`'s correct 46px due to cascade order; `.locale-toggle button` (`globals.css:204-210`) has no `min-height` at all. Fix both to `min-height: 44px`.

## 3. Security fix — XLSX total decompressed-size bound

`packages/shopline/src/bulk-form-xlsx.ts` already bounds each zip entry's decompressed size via zlib's native `maxOutputLength` option (`MAX_INFLATED_BYTES = 64 * 1024 * 1024`, confirmed present and real — not a post-hoc check, zlib itself aborts inflation past this). What's missing: a bound on the *sum* across all entries in the archive combined — `readZipEntries` decompresses every central-directory entry unconditionally, so a many-small-entries archive (each under the 64MB per-entry cap) isn't bounded by any total-size check, only by the implicit 65,535-entry-count ceiling and whatever the upload's own size limit is upstream. Add `MAX_TOTAL_INFLATED_BYTES = 96 * 1024 * 1024` (1.5× the per-entry cap — generous for a legitimate multi-sheet/multi-part workbook, while still bounding a pathological many-entry archive; the real Opak workbook this system targets is 182KB compressed, several orders of magnitude under this), tracked as a running total during `readZipEntries`, throwing the same `BulkFormWorkbookError` the per-entry check already uses.

## 4. Correlation IDs

No new table or column — `packages/db/src/schema.ts`'s `auditEvents.metadata` is already a schema-less `jsonb` column (confirmed: `id`/`workspaceId`/`actorId`/`entityId`/`action`/`metadata`/`createdAt`, no fixed shape on `metadata`). Generate one correlation ID (a plain random ID, matching this codebase's existing `generateId`-style helpers) at the point a batch or import begins, and thread it through `metadata.correlationId` on every audit event in that lifecycle — grounded in the real, confirmed action vocabulary already in use: `listing.imported` / `listing.import_refreshed` → `enrichment_batch.created` / `enrichment_batch.advanced` → `listing.approved` → `listing.bulk_export_created` / `listing.bulk_form_exported` / `listing.csv_exported`. Surface it as a new column in `/jobs`' ledger view (`apps/web/lib/jobs-ledger.ts`'s `LedgerEntry` type gains an optional `correlationId: string | null`, read from each source row's own metadata where present).

## 5. Metrics

Also derived from `audit_events` — no new table. A new aggregate query (`packages/db/src/repositories/audit.ts` gains a `countByReason` or similar method) counts specific `action`/`metadata.reason` combinations within a time window: retry counts from `listing.publish_failed`, version-conflict counts from existing 409 rejections (surfaced via a new `listing.review_conflict` audit event — not currently written; add one write site in the existing 409-handling path), stale-source rejection counts and partial-import counts from `listing.imported`'s existing metadata (which already records `refreshedProducts`/`issueCount` per earlier research this session — confirm the exact shape before implementing). Surfaced as new tiles on `/jobs` (matching its existing per-lane count-badge pattern) rather than a new dashboard.

## 6. Testing

- Accessibility: a jsdom/happy-dom test per fix confirming the specific DOM property (skip link href, single `<h1>` per rendered auth page, `aria-describedby` pointing at a real element, disabled-checkbox label containing the item title, table `aria-label` present) — matching this session's established DOM-testing conventions, not a new tool.
- Touch targets: a CSS-level test isn't practical the same way (no computed-style assertion tooling in this test suite) — verify by reading the rule directly (`min-height: 44px` present) plus a manual browser check at both breakpoints, matching how earlier packages this session verified CSS fixes.
- XLSX bound: a new test constructing a synthetic multi-entry archive whose total decompressed size exceeds 96MB but where every individual entry stays under 64MB, asserting `BulkFormWorkbookError` is thrown — proving the *total* bound is real and distinct from the existing per-entry one.
- Correlation IDs: an integration test asserting the same `correlationId` appears in an import event's metadata and its resulting batch-creation event's metadata.
- Metrics: unit tests for the new aggregate query against a fixture with a known mix of event types, asserting exact counts per metric.

## 7. Explicitly out of scope

- The pre-existing, dev-mode-only 500 on `/api/auth/register`'s local `next dev` module resolution — confirmed unrelated to any package's actual code (untouched by the H/I merge), confirmed absent from `next build` (what CI/Vercel actually run) — already flagged as its own separate follow-up task, not part of this package.
- A dedicated "stale-version conflict" UI on the review page — the existing generic `role="alert"` error handling already surfaces a 409 correctly (confirmed via direct code read); a bespoke "someone else edited this, reload?" affordance would be a UX enhancement, not an accessibility or security gap, and isn't called for by any confirmed audit finding.
- Any change to Better Auth configuration (already resolved via documentation in Package C, not reopened here).
- Any of the items already confirmed done in the audit: contrast ratios (all 5 real token pairings pass WCAG AA), keyboard focus-trap/restoration in the mobile drawer (already implemented and tested), icon-only action naming (no icons exist anywhere in this codebase), log redaction in Packages D–I's own logging (clean), cross-workspace RLS test coverage for every table that currently exists (enrichment batches, confirmation ledger, platform products, export attempts — all confirmed to have real isolation tests), and `listing-fields-form.tsx`'s existing field-metadata `aria-describedby` association (already correct).

## 8. Self-review

- **Placeholder scan:** none — every fix names its exact file, line, and concrete replacement; the two new-infrastructure items (§4, §5) name the exact existing mechanism (JSONB metadata, no migration) they build on rather than leaving the storage question open.
- **Internal consistency:** §1's three-group framing (accessibility / security / observability) is reflected exactly in §2–§5's structure and in §7's out-of-scope list, which explicitly cross-references the audit's own "confirmed done" findings rather than silently omitting them.
- **Scope check:** ten real, independently-confirmed items across three related but distinct areas — comparable in shape to this session's other M/L-sized packages, appropriately sized for a single implementation plan given each item is individually small.
- **Ambiguity check:** the two points with more than one reasonable resolution (observability scope, `loading.tsx` coverage) were both resolved explicitly with the user before this document was written, with reasoning recorded in §1 and §2 rather than left implicit.
